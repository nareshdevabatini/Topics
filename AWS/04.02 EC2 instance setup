Create EC2 instance and select Launch Instance
Write name and select OS type
Create a new security pair(Pair type: RSA, Private key file Format: pem) and save it
Select Advance details and add User data

#!/bin/bash
# Use this for your user data (script from top to bottom)
# install httpd (Linux 2 version)
yum update -y
yum install -y httpd
systemctl start httpd
systemctl enable httpd
echo "<h1>Hello World from $(hostname -f)</h1>" > /var/www/html/index.html

Lauch Instance
