public static void Main(string[] args)
        {
            
            var dta = Enumerable.Range(1, 100).Where((x)=> x %2 != 0 );
            foreach(var i in dta){
              Console.WriteLine(i);
            }
            
            
        }
