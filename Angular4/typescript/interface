let drawPoint=(point:{x:number,y:number})=>{
    //
}
drawPoint({x:1,y:1});
If above point has no of proporties then use interface

interface Point{
    x:number,
    y:number
}

let drawPoint=(point:Point)=>{
    //
}
drawPoint({x:1,y:1});
